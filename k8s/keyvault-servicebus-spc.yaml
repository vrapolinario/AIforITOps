apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: keyvault-servicebus
  namespace: ai-demo
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "true"
    userAssignedIdentityID: ${MANAGED_IDENTITY_CLIENT_ID}
    keyvaultName: ${KEY_VAULT_NAME}
    objects:  |
      array:
        - |
          objectName: servicebus-connectionstring
          objectType: secret
          objectAlias: ServiceBusConnectionString
    tenantId: ${TENANT_ID}